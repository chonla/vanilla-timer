import { InvisibleSliderDirective } from './invisible-slider.directive';

describe('InvisibleSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new InvisibleSliderDirective();
    expect(directive).toBeTruthy();
  });
});
